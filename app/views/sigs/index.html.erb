<h1>SIGs</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>
 
<% @resources.each do |resource| %>
  <tr>
    <td><%= resource.name %></td>
    <td><%= link_to 'Edit', edit_sig_path(resource) %> <%= link_to 'Delete', resource, :confirm => 'Are you sure?', :method => :delete if (resource.events == nil || resource.events.empty?) %></td>
  </tr>
<% end %>
  <tr>
    <td><%= link_to 'New SIG', new_sig_path %></td>
    <td/>
  </tr>
</table>